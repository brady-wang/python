Thinkphp3.2 入口绑定问题记录
// 应用入口文件
// 检测PHP环境if(version_compare(PHP_VERSION,'5.3.0','<'))  die('require PHP > 5.3.0 !');header("content-type:text/html;charset=utf-8");//定义常量define("SITE","http://localhost/");//define('BIND_MODULE','Home');//define('BIND_MODULE','Admin');// 开启调试模式 建议开发阶段开启 部署阶段注释或者设为falsedefine('APP_DEBUG',TRUE);
// 定义应用目录define('APP_PATH','./App/');
// 引入ThinkPHP入口文件require './ThinkPHP/ThinkPHP.php';
// 亲^_^ 后面不需要任何代码了 就是如此简单
记录:
1.入库绑定后再删除,就是如上面都注释掉  ,那么后面访问就通过模块来访问 后面配置文件里面配置一个默认模块 
http://localhost/tp/index.php/Home/Index/index
http://localhost/tp/index.php/Admin/Index/index
 
2.如果绑定的代码不取消,只能保留一个绑定,并且后面不能通过模块的方式来访问,因为它已经绑定一个默认的了
http://localhost/tp/index.php/Index/index
访问的时候不用加模块了
3.可以再新建一个admin.php 并且绑定模块Admin 之后面就都不用写模块了,两个入口文件就可以了
