linux分区
主分区加扩展分区不能超过四个
三个主分区，一个扩展分区
扩展分区不能放数据，放逻辑分区，一般放23个逻辑分区
 
格式化是为了写入文件系统
fat16只支持2gb分区
fat32支持32t，单个文件不能超过4g
ntfs 支持更大分区，更大单个文件
ext2
ext3
ext4  centos6.3
文件系统格式化干了两个事：主要将整个分区分为等格式化，4kb一个数据块 block
如10kb的文件放入了4kb 4kb 2kb 不能放其他文件 最小文件默认是4kb
可以更改最小的block
还有个分区列表 记录文件id号  修改时间 权限 数据保存到哪个块
i节点 node
放了衣服不能打隔断，先清空数据再写入文件系统 不仅仅是为了清空数据
 
